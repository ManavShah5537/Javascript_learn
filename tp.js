console.log(true,false);
let names = ['manav','nirav','shivali'];

let result = names.includes('mv');

console.log(result);

for(let i=0;i<names.length;i++)
{
    let html=`<div>${names[i]}</div>`;
    console.log(html);
}